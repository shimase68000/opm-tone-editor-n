# OPM Tone Editor 'Ｎ' version 1.20

[English](RELEASES.md) | 日本語

**更新日:** 2026年8月22日  
**作成者:** UG.

---

## 概要

Tone Editor v1.20 では、演奏モジュール **scalekey** との連携により、
キーボード・MIDI 入力で**ユニゾン・ディレイ・ディチューンを使った演奏**が可能です。

あわせて、マウス操作、ロック機能、ファイル入出力の各方面を見直し、
多数のバグ修正と信頼性の向上を行いました。

---

## 1. 新機能・機能拡張

### 演奏機能（scalekey 連携）

演奏モジュール scalekey と連携し、編集中の音色をその場で鳴らして確認できます。

- **ユニゾン演奏（UNISON 0〜8 音 [0:Mute]）** に対応しました。
  あわせてキーオンのタイミングをずらす **ディレイ** と、音程（発音周波数）をずらす **ディチューン** を搭載しました。
- OPM チャンネルにアサインする際の基点となる **アサイン開始チャンネル** を指定できます。
  また、OPM チャンネル数を増減すると、発音数（POLY 数）も自動で追従します。
- 発音チャンネルのアサイン方式を、**Sequential（SEQ）/ RoundRobin（RDR）** の
  それぞれについて **先着優先（HOLD）/ 後着優先（OVER）** で切り替えられます。
- **Slotmask** に対応しました。
  キーオンする operator を音色ごとに指定できます。

### エディタ操作

マウス操作を拡張しました。

- 通常クリックとSHIFT+クリックによる値操作

  | 対象 | 通常クリック | SHIFT＋クリック |
  |------|------|------|
  | ディチューン値 | ±1/64 半音 | ±半音 |
  | ディレイ値・ボリューム値 | ±1 | ±10 |
  | ノートオフセット値 | ±1 | ±12 |

- CTRL+クリックによる値のリセット操作
  - ディチューン値・ディレイ値 … **CTRL＋クリック** で 0 にリセット
  - ボリューム値 … **CTRL＋左 / 右クリック** で既定値（127 / 0）へ
  - ノートオフセット値 … **CTRL＋クリック** で初期値にリセット
- **OPM コピー UNDO**
  OPM パラメータテーブル間のコピー（△△△ / ▽▽▽）を 1 段階戻せます。
  コピー後に表示される **[UNDO]** ボタンで、コピー前の状態に復元します。
- **強制ロック（CTRL＋!）**
  編集済みの音色・ファイルでも、**CTRL＋!** で強制ロック可能です。
  強制ロックされた対象は、黄色の `!` で表示されます。

### 表示

- **メイン / サブ カーソル表示**
  OPM パラメータテーブルのうち、フォーカスされていない側（サブ）の現在音色を、
  音色名の右端を反転させて示します。
- **音色ロックマーク表示**
  - 通常ロック … 水色の `!`
  - 強制ロック（編集済み＋ロック）… 黄色の `!`
- **EXEC メニュー項目追加**
  - **DISP Sett** … 各種設定の現在値を表示します
  - **DISP SKEY** … 連携中の scalekey のバージョンを表示します
- **EXEC メニュー機能拡張**
下記メニューは、右クリックで実行することにより引数・ファイル名を編集可能です。
  - OED / SND / MML / OPM、EXEC1～4、DISP Fsts
- **アルゴリズム記号（OTG表記）**
  - 自己フィードバックを持つ op1 を bold で表示します。

### ファイル入出力・設定

- SND / MDX / MML / OPM / ZMS の読み込み時、
  tone memo に **ファイル名＋（音色名）＋音色番号** をインポートします。
  読み込み元の情報が音色メモの初期値になります。
- **読み込んだ音色を自動的にエクスポート選択状態にする**設定を追加しました
  （`auto_select_imported_tones`）。フォーマットごとに `oe.jsn` で設定できます。
- **マウスオーバーヘルプ**（画面の各要素にマウスを合わせると説明を表示する機能）の
  有効 / 無効を、`oe.jsn` で切り替えられるようになりました（既定：有効）。
- 起動引数に **jsn ファイル** を指定すると、その設定ファイルを読み込みます。
  指定しない場合は、実行ファイルと同じ場所にある jsn ファイルを読みます。
- `oe.jsn` に設定項目を追加しました。
  - `scalekey.polyphony` … 初期 POLY 数
  - `scalekey.delay_count` … 初期ディレイカウント
  - `scalekey.channel_assign_priority` … チャンネルアサインの先着優先 / 後着優先
  - `scalekey.start_scan_channel` … アサイン開始チャンネル
  - `offset_export_tone_number` / `offset_export_template_number` … エクスポート時の音色番号オフセット
- `oe.jsn` の記述がより自由になりました。
  `true` / `false`、`"enable"` / `"disable"`、`"on"` / `"off"`、`"yes"` / `"no"`
  などを真偽値として指定できます。

---

## 2. 挙動変更

v1.20 で動作が変わった点です。

- **ファイルロックと音色ロックを分離**しました。
  v1.10 では両者が連動していましたが、v1.20 では独立して動作します。
- 音色編集時に**編集マーク `*` を付与する条件を見直しました**。
  詳細は同梱の説明書（OE120.TXT）をご覧ください。
- `oe.jsn` の**一部の設定値の指定方法が変わりました**。
  以前の版で作成した jsn ファイルをお使いの場合は、リリースアーカイブに同梱の oe.jsn
  を編集して使うか、設定の書き方をご確認ください。

> ロック / 編集の状態や、ファイル操作の詳しい仕様は、
> 同梱の説明書（OE120.TXT）で説明しています。

---

## 3. バグ修正・信頼性の向上

多数のバグ修正と、ファイル入出力の信頼性向上を行いました。\
主なものは次のとおりです。

- ファイル保存・読み込み処理を全面的に見直して信頼性を向上しました。
- ファイルの読み込みに失敗したとき、**作業中の音色データが失われないように**しました。
- 異常なファイル・壊れたファイルを、より正確に検出して弾くようにしました。
- OPM 形式の書き出しで、AMS 値のところに AMD 値が書き込まれてしまう不具合を修正しました。
- 発音ノート表示を X68000 の OPM クロック（4.00MHz）に合わせ、ノートオフセット既定値を **+48 → +45** に修正しました。

---

## 4. 制限事項・動作条件

- 演奏機能を利用するには、**scalekey v1.10 以降**が必要です。
  scalekey は別途リリースしています（[scalekey Releases](https://github.com/shimase68000/scalekey/releases) から入手してください）。
- 1 つの scalekey を複数の Tone Editor から使用することはできません。
  （2 つめ以降の Tone Editor では scalekey が常駐していても演奏機能が使えません）
- テンプレート（TED）単体の読み込みには対応していません。
- 動作確認は X68000 エミュレータ環境で行っています。

---

<footer>
<p align="center">Copyright (c) 2026 UG. All rights reserved.</p>
</footer>
